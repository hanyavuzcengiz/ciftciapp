describe("Feature: Auth flow", () => {
  describe("Scenario: register -> otp -> token -> protected -> logout", () => {
    it("should complete full happy path", async () => {
      expect(true).toBe(true);
    });
  });
});
